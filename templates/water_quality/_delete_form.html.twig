<form method="post" action="{{ path('water_quality_delete', {'id': water_quality.id}) }}" onsubmit="return confirm('Voulez-vous supprimer ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ water_quality.id) }}">
    <button class="btn">Supprimer</button>
</form>
